using UnityEngine;
using System.Collections;
using UnityEngine.UI;

// Circle script
public class MoveUp : MonoBehaviour {
	/*
	public float minspeed = 50f, maxspeed = 250f;
	public enum CircleMode {Red, Green, Blue};
	public CircleMode mode; 

	float speed;

	void Start () {
		float sc = Random.Range (0.6f, 1.4f);
		Vector2 scaler = GetComponent<RectTransform> ().sizeDelta;
		scaler *= sc;
		GetComponent<RectTransform> ().sizeDelta = scaler;

		speed = Random.Range (minspeed, maxspeed);

		if(mode == CircleMode.Green) {
			GetComponent<Image> ().color = new Color (0, Random.Range (0.4f, 1f), 0, Random.Range (0.3f, 0.6f));
		}
		else if(mode == CircleMode.Red) {
			GetComponent<Image> ().color = new Color (Random.Range (0.4f, 1f), 0, 0, Random.Range (0.3f, 0.6f));
		}
		else if(mode == CircleMode.Blue) {
			GetComponent<Image> ().color = new Color (0, 0, Random.Range (0.4f, 1f), Random.Range (0.3f, 0.6f));
		}

	}
	
	// Update is called once per frame
	void Update () {
		GetComponent<RectTransform> ().Translate (new Vector2 (0, speed * Time.deltaTime));
	}*/
}
